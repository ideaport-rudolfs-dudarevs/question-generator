export enum VARIANT {
  DEFAULT = "default",
  SELECT = "select",
  CHECKBOX = "checkbox",
  RADIO = "radio",
}

export enum TYPE {
  TEXT = "text",
  EMAIL = "email",
  PASSWORD = "password",
  NUMBER = "number",
  DATE = "date",
}

export enum BTN_TYPE {
  BUTTON = "button",
  SUBMIT = "submit",
  RESET = "reset",
}
